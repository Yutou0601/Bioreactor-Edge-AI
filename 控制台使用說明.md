# 桌面控制台使用說明

雙擊 `control_panel.pyw` 即可開啟。現場操作不需要開終端機、不需要記指令。

## 為什麼要有這個

2026-07-22 監控電腦執行 Windows 自動更新，洗掉記錄程式，資料**靜默中斷 17.5 小時**
無人察覺（反應器本身仍在運轉，只是沒人在記）。控制台針對這件事設計：

- **資料新鮮度是第一級指標**——「後端活著」不等於「資料有在進來」。後端可能好端端
  跑著，但記錄來源早就斷了。控制台把這兩件事分開顯示，資料逾時會轉紅告警。
- **開機自動啟動**——機器被強制重開後系統會自己回來，不必等人到現場。

## 畫面

| 狀態列 | 意義 |
|---|---|
| 後端 API | 後端是否在 :8000 監聽、已運行多久、記憶體內幾筆資料 |
| **資料記錄** | **最後一筆資料距現在多久**。逾 5 分鐘轉紅並顯示停止進資料多久 |
| Jetson 連線 | 免密碼登入是否已設定（決定能否遠端啟動後端） |
| CSV 監看 | csv_watcher 是否由本控制台啟動中 |
| 網頁前端 | vite preview 是否在 :4173 監聽 |
| 版本列 | 目前 commit，以及是否落後 origin（落後時轉黃提示） |

## 後端跑在哪裡

**正式部署時後端在 Jetson 上**（`web_frontend/.env.production` 指向
`192.168.55.1:8000`），監控電腦只跑 CSV 監看與網頁前端。因此控制台預設把
後端位置設為 Jetson，「啟動／重啟後端」都是透過 SSH 遠端執行。

按「切換後端位置」可在 **Jetson（正式）** 與 **本機 127.0.0.1（開發）** 間切換。

**前端會跟著一起切換**，這點很重要：

| 後端位置 | 前端跑法 | 網址 | 網頁打哪台 |
|---|---|---|---|
| Jetson（正式） | `npm run preview` | :4173 | `.env.production` → 192.168.55.1 |
| 本機（開發） | `npm run dev` | :5173 | `.env.development` → localhost |

正式建置的前端把 API 位址**寫死**在 `dist` 裡指向 Jetson。若在本機模式下開
:4173 的頁面，網頁會去打 Jetson 而顯示「後端無回應」——即使本機後端好好跑著。
控制台已自動配對兩者，不會弄錯。

## Jetson 免密碼登入（第一次必做）

**為什麼不能把密碼寫在程式裡**：Windows 內建的 `ssh.exe` 不接受用參數或管線
餵密碼——它直接從終端機讀取。控制台是隱藏視窗＋接管輸出，密碼提示會**無聲卡死**，
比跳出來要你打還糟。這台也沒有 `plink`／`sshpass`。

所以用 SSH 金鑰，這也避免把密碼存在任何檔案裡：

1. 按 **「設定 Jetson 免密登入」**
2. 沒有金鑰的話會自動產生（`~/.ssh/id_ed25519`，不設通行碼）
3. 跳出一個命令視窗安裝公鑰 → **在該視窗輸入 Jetson 密碼，只需要這一次**
4. 完成後「Jetson 連線」轉綠，之後所有遠端操作都不再需要密碼

設定好之前，控制台的 SSH 一律帶 `BatchMode=yes`——連不上就**立刻報錯**，
絕不卡在看不見的密碼提示。

## 按鈕

- **全部啟動**：依序拉起後端（遠端則走 SSH）→ CSV 監看 → 網頁前端。
  已在跑的會自動略過，可重複按。
- **停止全部**：停掉**本機**服務（會再問一次）。
  ⚠ **Jetson 上的後端不會被停止**——它正在記錄資料，靜默停掉正是要避免的事故模式。
- **重啟後端**：先釋放 8000 埠（遠端則在 Jetson 上釋放），再重新拉起。
  ⚠ 重啟會清空記憶體資料，需要時請重新「匯入 CSV」。
- **檢查更新**：`git fetch` 看有無新版；有的話詢問後執行
  `git pull` → 安裝套件 → 重建前端 → 重啟後端，全程輸出串到日誌。
- **開啟網頁**：瀏覽器開啟 http://localhost:4173
- **匯入 CSV**：選資料夾後跑 `batch_import_csv.py`。**選過的資料夾會被記住**，
  之後「全部啟動」就會一併拉起 CSV 監看。

## 開機自動啟動

勾選後會在 Windows 啟動資料夾寫入 `bioreactor_autostart.bat`，開機時以
`--autostart` 參數叫起控制台並自動拉起所有服務。取消勾選即刪除該檔。

用 `.bat` 而不是捷徑，是為了讓它是純文字、看得懂、要手動移除也容易：

```
%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\bioreactor_autostart.bat
```

## 首次使用

1. 確認 Jetson 已連線（USB-C 或網路），可 ping 到 `192.168.55.1`
2. 開控制台 → **設定 Jetson 免密登入**（輸入一次密碼）
3. **全部啟動** → 確認「後端 API」與「資料記錄」皆為綠
4. **匯入 CSV** 選定感測器資料夾（會記住，之後啟動會一併拉起 CSV 監看）
5. 需要的話勾選**開機自動啟動**

若只是在開發筆電上測試，第 1、2 步略過，先按「切換後端位置」改成本機，
並確認 `edge_backend\venv` 存在（沒有的話控制台會提示建立指令）。

## 注意

- 關閉控制台會一併停止它啟動的服務（會先問）。只是想收起視窗、讓記錄繼續跑的話，
  請**最小化**而不是關閉。
- 本控制台只用 Python 標準函式庫（tkinter），不需額外安裝套件。
- `control_panel.json`（記住的 CSV 路徑）不進版控，各台機器各自設定。
