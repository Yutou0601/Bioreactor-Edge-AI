# 系統架構圖（draw.io / mermaid）2026-07-27

## 匯入方式（draw.io）
1. 開 [app.diagrams.net](https://app.diagrams.net)
2. 功能表 **Arrange（排列）→ Insert（插入）→ Advanced（進階）→ Mermaid…**
3. 貼下方整段 mermaid 程式碼 → **Insert** → 自動生成可編輯圖形

> 部署對應實際情況：**後端跑在 Jetson（192.168.55.1:8000）**，監控電腦只跑記錄程式、CSV 監看與網頁前端。

```mermaid
flowchart TB
    subgraph PHY["生物反應器（現場）"]
        R["微正壓循環反應槽<br/>CO2 + 4H2 → CH4 + 2H2O"]
        S["感測器<br/>反應槽壓力 / 混合槽壓力 / ORP / pH（可信）<br/>CO2 / CH4（僅參考·99.98% 無效拖尾）"]
        HMI["Delta HMI 觸控面板"]
        R --> S
        R --> HMI
    end

    subgraph PC["監控電腦（Windows）"]
        REC["BTP.SerialHarbor1.1.exe<br/>記錄程式（每分鐘一筆）"]
        CSV[("BTP_Sensor_log-*.csv / .txt<br/>逐日檔")]
        WATCH["csv_watcher.py<br/>ORP EMA 前處理 + 壓力欄位對調"]
        IMPORT["batch_import_csv.py<br/>歷史回補（HTTP /import_csv，去重）"]
        CP["control_panel.pyw<br/>健康監看 / 啟動服務 / SSH 免密"]
        FE["Vue3 前端（vite）<br/>MonitorView 時序圖 · ExperimentView 批次面板"]
        REC --> CSV
        CSV --> WATCH
        CSV --> IMPORT
    end

    S -->|序列埠| REC

    MQTT{{"MQTT Broker<br/>reactor/01/sensors<br/>reactor/01/prediction"}}
    WATCH -->|publish 逐分鐘感測| MQTT

    subgraph JET["Jetson Orin Nano（邊緣後端 :8000）"]
        MC["core/mqtt_client.py<br/>訂閱 + 即時推論"]
        MEM[("sensor_records<br/>記憶體逐分鐘資料")]
        API["FastAPI（main.py / api/routes.py）<br/>REST /api/*<br/>標籤+逗號格式解析·記錄中斷分段"]
        subgraph ANA["分析模組（知識驅動）"]
            ES["experiment_store<br/>循環偵測(谷底) / 下降速率 / 質傳 k,Peq / ORP 回升"]
            COV["co2_covariate_association<br/>斜率 ↔ ORP / pH / 補氣次數（叢集穩健SE）"]
            GB["co2_greybox_identifiability<br/>profile likelihood / 分離就緒"]
            CH4["ch4_realtime<br/>XGBoost + 特徵歸因（背景執行）"]
        end
        MC --> MEM
        IMPORT -->|HTTP POST| API
        API --> MEM
        MEM --> ES
        ES --> COV
        ES --> GB
        API --> CH4
    end

    MQTT -->|subscribe 感測| MC
    API -->|publish 預測| MQTT
    MQTT -->|subscribe 預測| FE
    FE -->|REST 查詢批次/分析| API
    CP -. SSH 遠端啟動/健康檢查 .-> JET
    CP -. 啟動 .-> WATCH
    CP -. 啟動 .-> FE

    classDef trust fill:#e8f5e9,stroke:#2e7d32;
    classDef ref fill:#fff8e1,stroke:#e0b000;
    classDef edge fill:#e3f0fb,stroke:#1f5fa8;
    class S trust;
    class MC,MEM,API,ES,COV,GB,CH4 edge;
```

## 圖中要點（給看圖的人）
- **可信訊號**（綠）：反應槽壓力、混合槽壓力、ORP、pH；CO2/CH4 僅參考。
- **邊緣運算**（藍）：推論與分析都在 Jetson 上，非雲端。
- **兩條資料路徑**：即時（csv_watcher → MQTT → Jetson）＋ 歷史回補（batch_import_csv → HTTP）。
- **控制台** control_panel.pyw 以虛線表示「編排/監看」而非資料流。
