# ICEA 2026 — 圖、表、演算法、LaTeX 待辦清單

**建立日期**：2026-08-05
**對應稿件**：`docs/ICEA2026_paper_draft_v1.md`
**總量**：3 張圖（1 新繪 + 2 重製）、8 張表、3 個演算法、4 類 LaTeX 檔案

---

## 一、圖（3 張）

### Fig. 1 — 系統架構圖　⬛ 不存在，要從零畫

- [ ] 繪製流程：sensors → edge ingestion → streaming cycle segmentation → joint estimation → bootstrap calibration gate → upstream reporting
- [ ] 需為**向量圖**（PDF / EPS），不可用點陣圖
- [ ] 工具建議：TikZ（可直接嵌 LaTeX，字型自動與內文一致）或 draw.io 匯出 PDF
- [ ] 字級印出後 ≥ 6 pt
- [ ] 黑白可辨識（不可只靠色塊區分模組）

### Fig. 2 — 兩步驟偏誤與校準式聯合擬合

- **來源**：`edge_backend/joint_fit_calibrated.py:263`
- **檔案**：`docs/analysis_charts_3batch/fig26_calibrated_joint_fit.png`
- **結構**：4 個 panel（`add_subplot` × 4）

- [ ] **中文標籤全部改英文**（見下方對照表）
- [ ] `savefig` 補上 `dpi=1200`（目前**沒帶 dpi 參數**，吃預設 100）
- [ ] 三批次改為「顏色 + 線型 + 標記」三重區分
- [ ] 確認字級印出後 ≥ 6 pt（現為 fontsize 8.5–10.5）
- [ ] 改輸出 PDF 向量格式

**中文標籤對照（Fig. 2）**

| 行號 | 現況（中文） | 建議英文 |
|---|---|---|
| L202 | `表觀 rb = {rb2}` | `apparent r_b = {rb2}` |
| L211 | `rb ≡ 0 的合成資料` | `synthetic data, r_b ≡ 0` |
| L213 | `實測 {rb2}` | `observed {rb2}` |
| L217 | `虛無中位 {…}（假 rb，比實測還大）` | `null median {…} (spurious r_b, exceeds observed)` |
| L226 | `rb ≡ 0 的合成資料` | `synthetic data, r_b ≡ 0` |
| L228 | `實測 {thj[4]}` | `observed {thj[4]}` |
| L232 | `虛無中位 {…}（幾乎無偏）` | `null median {…} (nearly unbiased)` |
| L242 | `物理溶解 kLa(P−Peq)` | `physical dissolution k_La(P − P_eq)` |
| L244 | `生物消耗 rb（三批共用）` | `biological consumption r_b (shared)` |
| L246 | `{share}%\n生物` | `{share}%\nbiological` |
| L254 | `圖26 兩步驟表觀參數迴歸的偏誤，與校準式聯合擬合` | *（圖底標題，LaTeX 用 `\caption` 取代，繪圖時移除）* |

### Fig. 3 — 自我辨識平台

- **來源**：`edge_backend/self_identifying_platform.py:161`
- **檔案**：`docs/analysis_charts_3batch/fig27_self_identifying_platform.png`
- **結構**：3 個 panel（`add_subplot` × 3）

- [ ] **中文標籤全部改英文**（見下方對照表）
- [ ] `savefig` 補上 `dpi=1200`
- [ ] 三批次改為「顏色 + 線型 + 標記」三重區分
- [ ] 確認字級 ≥ 6 pt
- [ ] 改輸出 PDF 向量格式

**中文標籤對照（Fig. 3）**

| 行號 | 現況（中文） | 建議英文 |
|---|---|---|
| L112 | `每個 ▼ = 控制器自動觸發的一次弛豫實驗` | `each ▼ = one controller-triggered relaxation experiment` |
| L121 | `實測 CV` | `observed CV` |
| L125 | `1/√n（獨立樣本理論）` | `1/√n (independent-sample theory)` |
| L129 | `實測冪次 {sl}\n理論 -0.50` | `observed exponent {sl}\ntheory −0.50` |
| L144 | `CV<10%\n需 {d} 天` | `CV < 10%\nrequires {d} days` |
| L151 | `圖27 自我辨識平台：…` | *（改用 LaTeX `\caption`，繪圖時移除）* |
| — | `時間` / `反應槽壓力 (kg/cm²)` | `Time` / `Reactor pressure (kg cm⁻²)` |

### 圖的共通注意事項

- [ ] 移除圖內建的中文標題列——LaTeX 的 `\caption` 才是正式圖說，圖內重複會被審稿人視為排版瑕疵
- [ ] 中文字型在英文環境會變成方框，**翻譯後要重跑一次確認沒有 tofu**
- [ ] 三批次目前為藍／橘／綠，**轉灰階極可能糊在一起**，這是必須處理而非可選

---

## 二、表（8 張）

Table 9 已在 §6 壓縮時併入正文，現為 8 張。全部要從 Markdown 轉 LaTeX `booktabs`。

- [ ] **Table 1** 反應器設定與本研究採用的專利功能子集（7 列）
- [ ] **Table 2** 三批次固定協定（3 列）
- [ ] **Table 3** 兩步驟表觀參數迴歸對自身虛無（4 列）
- [ ] **Table 4** 兩個估計量在同一組 r_b ≡ 0 合成資料上的偏誤（2 列）
- [ ] **Table 5** 校準後參數與留一循環穩定度（5 列）
- [ ] **Table 6** 通道分解（3 列）
- [ ] **Table 7** r_b 精度學習曲線（5 列，若超頁可縮為 3 列）
- [ ] **Table 8** 兩平台管線成本（6 列）

**轉檔注意**

- [ ] 用 `booktabs`（`\toprule` / `\midrule` / `\bottomrule`），不畫直線——Springer 慣例
- [ ] 表標題置於表**上方**（`\caption` 在 `\begin{tabular}` 之前）
- [ ] 上標與下標改 LaTeX 數學式：`kg cm⁻²` → `kg\,cm$^{-2}$`、`r_b` → `$r_\mathrm{b}$`、`k_La` → `$k_\mathrm{L}a$`
- [ ] 負號用數學模式 `$-$`，不可用連字號

---

## 三、演算法（3 個）

目前是 Markdown code block，要轉成 `algorithm2e` 或 `algorithmic` 環境。

- [ ] **Algorithm 1** Streaming cycle segmentation（§4.2）
- [ ] **Algorithm 2** Joint estimation across batches（§4.3）
- [ ] **Algorithm 3** Bootstrap calibration of the null（§4.4）

**注意**

- [ ] 三個設計理由（30 分鐘間隔門檻、20 分鐘補氣合併、谷底端點）必須留在正文，不可只寫在虛擬碼註解裡
- [ ] 若超頁，Algorithm 1 可壓成 6 行

---

## 四、LaTeX 檔案（4 類）

- [ ] **`llncs.cls`** — 從 Springer 官方下載（不可用第三方複製版）
- [ ] **`splncs04.bst`** — 同上；此樣式**依第一作者姓氏字母排序並自動重新編號**
- [ ] **`main.tex`** — 灌入 8 節正文 + 8 表 + 3 演算法
- [ ] **`references.bib`** — 10 筆，均已於 2026-08-05 經 CrossRef／PubMed 查證

**建置注意**

- [ ] 正文引用**一律用 `\cite{key}`，不可寫死數字**——`splncs04.bst` 會重排順序
- [ ] 作者區塊填入：Cheng-Yu Li¹（通訊）、Chun-Hao Chen¹、Cheng-Yuan Hung²
- [ ] 保留 Disclosure of Interests 段（專利 TW I923176 屬金屬中心，共同作者隸屬該單位）
- [ ] Acknowledgments 的**經費來源與計畫編號目前是空的**，須填
- [ ] 編譯順序：`pdflatex → bibtex → pdflatex → pdflatex`

---

## 五、建議執行順序

**先做 LaTeX，再做圖。** 理由：目前頁數估算在 9.2 到 12.1 頁之間浮動（差 3 頁），無法用來決定要刪什麼。LaTeX 一旦編出來就有真實頁數，而且會直接決定 **Fig. 2/3 要不要縮成單欄**——先畫圖可能白做。

1. [ ] **建 `main.tex` + `references.bib`**，表與演算法順手一起轉 → **取得真實頁數**
2. [ ] 依真實頁數決定是否執行刪減（順序見進度報告 §二）
3. [ ] **Fig. 2 / Fig. 3 重製**（翻譯 + dpi=1200 + 黑白可辨識 + PDF 向量）
4. [ ] **Fig. 1 從零繪製**（TikZ 或 draw.io）

---

## 六、本清單未涵蓋、但同樣擋投稿的項目

詳見 `docs/論文進度報告_2026-08-05.md`：

- [ ] 三位作者的 **ORCID**、兩校**正式英文地址與郵遞區號**、共同作者信箱
- [ ] Acknowledgments 的**經費來源與計畫編號**
- [ ] 利益揭露聲明**措辭經洪博確認**
- [ ] 參考文獻補至 12–18 篇（**每筆都要先 DOI 查證**）
- [ ] Jetson Orin Nano 實測 → Table 8 補第三欄
- [ ] Copyright form 手簽（Springer 不收數位簽章，需郵寄時間）
