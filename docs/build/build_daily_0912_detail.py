# -*- coding: utf-8 -*-
"""把 detail_tables.py 的輸出組成明細版日報。

⚠ 表格一律由程式帶入，不手抄。先前手抄已經出過錯（份額 38% vs 37%±1%），
  而這份有六張表、上百個數字。
"""
import io
import os
import re
import sys

# 本檔在 docs/build/ 底下，往上兩層是 repo 根目錄
REPO = os.path.dirname(os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
OUT = os.path.join(REPO, 'docs', 'reports', '日報_2026-09-12_明細表.md')

# 表格直接取自 detail_tables 的結構化資料。
#
# ⚠ 原本是「跑 detail_tables.py、再用正規式從文字輸出裡切出各表」。那個做法
#   在 2026-09-12 重構（資料／呈現分離）後當場壞掉——標題由「表 A」變成
#   「表 A　τ=10 批次：…」，`【表 ([A-F])】` 就比對不到了。
#   解析自己程式的文字輸出是脆弱的，改成 import 同一份資料。
#   Word 版（build_daily_0912_detail_docx.py）也走同一份。
sys.path.insert(0, os.path.join(REPO, 'research', 'cycles'))
from detail_tables import DATA, render_text                    # noqa: E402

T = {k: render_text(DATA[k]()) for k in 'ABCDEF'}
assert sorted(T) == list('ABCDEF'), sorted(T)


def block(k):
    return '```\n' + T[k] + '\n```'


DOC = """| 國立高雄科技大學_金屬中心_日報（明細表） |  |  |  |
|---|---|---|---|
| 時間 | 2026/09/12 | 學生 | 李承育 |
| 地點 | 金屬中心（全部分析重跑，逐段／逐分鐘／逐日明細） |  |  |

> 本份為同日另一份日報的**明細版**。所有分析重新跑過一次，數字由
> `research/cycles/detail_tables.py` 直接產生，未經手抄。
> 該腳本可重跑：`python detail_tables.py`（或只要某幾張表 `python detail_tables.py A C`）。

---

## 目錄

| 表 | 內容 | 回答什麼問題 |
|---|---|---|
| A | τ=10 批次每一段下降的完整明細 | 每個循環各降多少、多久、有多直 |
| B | 循環泵：小時內逐分鐘壓降（三批對照） | 泵什麼時候開、開多久 |
| C | 自動循環的內部剖面（13 段疊加） | 循環內部速率怎麼變化 |
| D | 自動化批次逐日明細 | 二十天裡每天發生什麼 |
| E | 化學計量：分階段與各種估計量 | 轉換了多少、誤差多大 |
| F | 三批次總表 | τ 槓桿的整體效果 |

---

## ⚠ 先更正一個先前報告的錯誤

**2026-09-11 週報裡那張逐日表的「當日消耗」欄是錯的。**

該欄先前以「所有負的相鄰壓力差加總」計算，把感測器 ±0.01 的抖動也算成了
消耗，數值被灌水約四倍（報成 2.0 ~ 3.3，實際是 0.42 ~ 0.89）。

證據是它與同一份報告的總量對不起來：舊值加總約 50，但全期總消耗是 13.47。
本份表 D 改用「以補氣為界切段、段首減段尾」計算，**逐日加總正好 13.47**，
與總量一致。

> 受影響的只有該欄。份額、CH4 產量、總消耗等其他數字均以正確方式計算，
> 不受影響。

---

## 表 A　τ=10 批次：每一段下降的完整明細

期間 2026-07-30 至 08-03。本表列出切出的**每一段**，不做篩選。

@@TABLE_A@@

**怎麼看這張表**

- **判定欄**由時長與降幅自動分類，不是人工標註。13 段「自動循環」的
  形狀高度一致；標★的是人工排氣或資料邊界。
- **R²** 是「這段下降有多接近一條直線」。自動循環都在 0.96 以上，人工排氣
  則掉到 0.44 ~ 0.48 —— 兩者形狀差異極大，可據此自動區分。
- 第 18 段是**真正的排氣動作**：5 分鐘內壓力由 0.77 掉到 0.18，
  速率 8.85 kg/cm²/hr，是自動循環的 236 倍。
- 第 1、16、19 段是資料邊界或短殘段，不是完整循環。

**與會議設想的對照**：會議說「1.2 開始下降到 1.1」，實測自動循環是
**1.17 → 0.92，降幅 0.252 ± 0.009**，耗時 6.83 ± 0.76 小時。
降幅是設想的 2.5 倍。若要改成「掉 0.1 就排氣」，依實測速率
0.0374 kg/cm²/hr 推算，節奏約為 **2.7 小時一次**。

---

## 表 B　循環泵：小時內逐分鐘的平均壓降（三批對照）

把每一筆與前一筆的壓力差，依「該筆落在小時內的第幾分鐘」分組平均。
補氣（單步跳升 > 0.03）已排除。

@@TABLE_B@@

**怎麼看這張表**

- 三批的泵窗**位置不同**（第 50、25、13 分），但**長度分別是 1、5、10 分**，
  與各批宣稱的 τ 完全一致。τ 的定義就是每小時循環幾分鐘。
- 「開」那一分鐘是**驟降尖峰**（泵一開，氣體大量進入液相）；
  「停」那一分鐘是**回升尖峰**（泵停止，壓力部分回彈）。
- 泵窗以外的分鐘，壓降幾乎都在 ±0.001 以內，且標準誤同量級 —— 亦即
  **不運轉時壓力幾乎不動**。
- tau5 的對比最極端：泵只佔每小時的 8.3% 時間，卻造成 **85%** 的壓降。

---

## 表 C　τ=10 自動循環的內部剖面（13 段相干疊加）

把 13 個自動循環按「循環內經過幾小時」對齊後疊加，每 30 分鐘一格。
疊加後的標準誤只有 0.002 ~ 0.006，可以看出單一循環看不見的結構。

@@TABLE_C@@

**怎麼看這張表**

「相對速率」欄是該 30 分鐘的速率除以全段平均。數值**明顯交替**：
1.71×、1.78×、0.37×、1.36×、0.43×、1.39×、0.36× ⋯⋯

這就是表 B 的泵節奏在循環內部的呈現：含有泵窗的那半小時速率高，
不含的那半小時速率低。**循環內的下降不是等速的。**

---

## 表 D　自動化批次：逐日明細（08-11 ~ 08-31）

@@TABLE_D@@

**怎麼看這張表**

- **建立期（8/11 ~ 8/23）**：甲烷逐日上升，日增幅由 +10.2 收斂到 +0.3，
  是典型的接近飽和；二氧化碳同步由 4.20% 單調降到 0.30%。
- **8/24 ~ 8/25 氫氣耗盡**：8/24 當日只補氣 **1 次**（平時 11 ~ 13 次），
  壓力掉到 0.69，甲烷跌 16.6 個百分點。與現場照片吻合。
- **衰退期（8/26 起）**：二氧化碳維持 **0.00%**，甲烷由 42.63% 一路降到
  31.27%，每日補氣次數與消耗量同步下滑。
- **8/31 覆蓋率僅 38%**，該日數值不宜單獨引用。
- 「當日消耗」欄加總為 **13.47**，與全期總消耗一致（見前述更正）。

---

## 表 E　化學計量：分階段與各種估計量

@@TABLE_E@@

**怎麼看這張表**

同一段資料用不同估計量會得到不同數字，差異來自 CH4 軌跡是**飽和曲線**
而非直線：

- **單筆端點**：只用頭尾各一筆，給不出誤差。
- **兩端各 6 hr**（建議）：夠平均掉雜訊，又不會取太寬而咬到曲率。
- **兩端各 12 / 24 hr**：末端平均取得越寬，越會把仍在上升的曲線拉低，
  估計值因此系統性偏小。

⚠ **氫氣耗盡期只有 2 天，三種估計量差異極大**（+29% / +39% / +67%），
不應引用該期的份額。

---

## 表 F　三批次總表（τ 槓桿）

@@TABLE_F@@

**怎麼看這張表**

下降速率隨 τ **單調上升**：0.0173 → 0.0325 → 0.0377 kg/cm²/hr。
泵窗長度同步為 1 → 5 → 10 分鐘。兩者的對應關係說明了 τ 這個槓桿的機制：
**泵運轉時間直接決定氣液接觸時間**。

⚠ **CH4 峰欄**顯示可用的濃度錨點極少：tau1 只有 1 個、tau5 **一個都沒有**、
tau10 有 2 個。沒有錨點就無法做化學計量歸因 —— 這是目前最大的限制。

---

## 綜合判讀

**1. 自動循環的一致性很高。** 13 段的降幅標準差只有 0.009（佔均值 3.6%），
時長標準差 0.76 小時。設備運轉穩定，資料品質足以支撐分析。

**2. 循環內的下降不是等速的，由泵主導。** 泵只佔每小時的 1.7 ~ 17% 時間，
卻造成 37 ~ 85% 的壓降。這一點對論文的模型假設有影響：估計器擬合的是
脈衝序列的**包絡線**，而非連續的底層動力學。

**3. 濃度錨點不足仍是主要瓶頸。** 三批合計只有 3 個 CH4 峰值。會議規劃的
「3 ~ 4 小時排一次氣」正是為了解決這一點。

---

## 待辦事項

1. **確認二氧化碳供應狀況**（延續前報，仍為最優先）。自 8/23 起頭空
   二氧化碳為 0.00%，8/26 之後甲烷持續下降。

2. **排氣期間記錄頻率提高到每 10 秒**。實測排氣只橫跨 3 ~ 4 筆資料。

3. **更正週報的逐日消耗欄**（本報告已列出正確值）。

4. 論文模型假設段落建議補充「所擬合者為脈衝序列之包絡線」。

## 待確認事項

1. 循環泵的啟動時刻三批各不相同（第 50、25、13 分）。此為控制程式設定，
   或由開機時刻決定？若為後者，該時刻應併入實驗紀錄。

2. tau5 批次期間**完全沒有可用的 CH4 濃度峰值**，是否該期間未曾排氣？

3. 延續前報：資料夾標示之「補氣 0.15 / 0.2 kg」對應之量測值；
   8/31 覆蓋率 38% 之資料是否可用。

## 附註

- 分析腳本：`research/cycles/detail_tables.py`（本報告全部表格）、
  `research/cycles/pump_rhythm.py`（泵節奏與 τ 驗證）、
  `research/cycles/hourly_resolution.py`（小時尺度可量測界線）
- 壓力單位皆為 kg/cm²（錶壓）；絕對壓 = 錶壓 + 1.033
- 體積換算以頭空 1.00 L、30 °C 計。⚠ 溫度欄全期恆為 30.00，非實測值
"""
for _k in 'ABCDEF':
    DOC = DOC.replace('@@TABLE_' + _k + '@@', block(_k))

io.open(OUT, 'w', encoding='utf-8', newline='').write(DOC)
print('寫入', OUT)
print('  %d 行、%d 字元' % (len(DOC.splitlines()), len(DOC)))
