# Web Server & API Framework
fastapi>=0.110.0
uvicorn>=0.27.1
pydantic>=2.6.3
python-multipart>=0.0.9   # /api/import_csv 檔案上傳（FastAPI File()）需要

# Data Processing & Math
# 原本用 == 釘死版本（numpy==1.24.4 等），但這些版本沒有新版 Python（3.12+）的
# 預編譯 wheel，會退回原始碼編譯並因 setuptools/pkg_resources 相容性問題失敗。
# 部署到金工電腦／Jetson 等未知 Python 版本的機器時容易卡住，改用下限版本讓
# pip 自動挑相容版本（本專案開發環境實測 numpy 2.5 / pandas 3.0 / scikit-learn 1.9 皆可正常運作）。
numpy>=1.24.4
pandas>=2.2.1
scikit-learn>=1.4.1
joblib>=1.3.2

# Hardware
pyserial>=3.5          # USB 感測器接收器

# HTTP client（sensor_simulator.py / batch_import_csv.py 用）
requests>=2.31.0

# MQTT
paho-mqtt>=2.0.0

# AI / Deep Learning (Jetson 請使用官方 CUDA 版本)
torch>=2.0.0

# 跨平台推論（x86 監控電腦 / Jetson 雙環境驗證用）
onnx>=1.16.0
onnxruntime>=1.18.0        # 僅適用 x86；Jetson (ARM64) 需改用 NVIDIA 官方對應 JetPack 版本的 wheel，不能直接 pip install 這個
onnxscript>=0.1.0          # torch>=2.7 預設的 dynamo-based ONNX exporter 需要